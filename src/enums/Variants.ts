enum Variants {
    DARK = 'dark',
    LIGHT = 'light',
    MOBILE = 'mobile',
    CP = 'cp',
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    DASHED = 'dashed',
    WHITE = 'white'
}

export default Variants;
